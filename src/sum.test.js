import { sum } from "./sum.js";

test('test function sum', () => {
    expect(sum(1, 2)).toBe(3);
});
test('test function sum2', () => {
    expect(sum(1, 2)).toBe(3);
});
test('test function sum3', () => {
    expect(sum(1, 2)).toBe(3);
});
test('test function sum4', () => {
    expect(sum(1, 2)).toBe(3);
});